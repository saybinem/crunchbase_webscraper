import common
import company
import person

#company_name = "ip-access"
#company_data = company.scrapeOrganization(company_name)
#
#if(company_data is not False):
#    for p in company_data['people']:
#        person.scrapePerson(p[1])

#person.scrapePerson("/person/dr-nick-johnson")
person.scrapePerson("/person/travis-kalanick")